package dev.kabin.util.events;

/**
 * Enum constants for key codes.
 */
public enum KeyCode {
    Q, W, E, R, T, Y, U, I, O, P, A, S, D, F, G, H, J, K, L, Z, X, C, V, B, N, M,
    SPACE,
    CONTROL_LEFT, SHIFT_LEFT, ALT_LEFT, ESCAPE,
    F12
}
